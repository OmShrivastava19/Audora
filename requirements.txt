# Root requirements - install dependencies for both frontend and backend development
# 
# Backend dependencies - see backend/requirements.txt
# Frontend dependencies - managed via npm/yarn in frontend/ directory
#
# To set up:
# 1. Backend: pip install -r backend/requirements.txt
# 2. Frontend: cd frontend && npm install
#
# This file is kept for compatibility but backend-specific deps are in backend/requirements.txt